## YAML Template.
---
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Laboratory
    username: postgres
    password: Nacionalcampeon04.
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: rayajax777@gmail.com
    password:  klzx ocmk uuii oday
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true