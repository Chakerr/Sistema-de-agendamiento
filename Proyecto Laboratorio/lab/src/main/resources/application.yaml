## YAML Template.
---
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Laboratory
    username: postgres
    password: Nacionalcampeon04.
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: laboratoristaupc@gmail.com
    password:  ezim tduj kezy gzld
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true